package sqlite_repo

import (
// "github.com/steams/salmon-hub/pkg/rand"
// "github.com/stretchr/testify/assert"
// "testing"
)

// func TestAdd(t *testing.T) {
// 	// Test that Add method on media service passes the apropriate values through to the repository

// 	// should test that adding any numer or order of media trhough service results in the repo containing appropriate values with appropriate ids
// 	// should test that adding only adds to the specific userid given

// 	repo := test_repo{make(map[string]Media)}
// 	media_service := CreateService(repo)

// 	var songs [][]Media
// 	var ids []string

// 	for(ids)
// 	for i := 0; i <= rand.Int(10); i++ {
// 		rand_song := Media{rand.String(7), rand.String(7), rand.String(7), rand.Int(100), rand.String(7), rand.String(7)}
// 		append(songs, rand_song)
// 	}

// 	// input := Media{"title", "artist", "album", 1, "playlist", "hash"}
// 	// id := "id"

// 	media_service.Add(id, input)

// 	assert.Equal(t, input, repo.added, "Media Service Should pass correct struct to repository")
// 	assert.Equal(t, id, repo.added_id, "Media Service Should pass correct id string to repository")
// }
